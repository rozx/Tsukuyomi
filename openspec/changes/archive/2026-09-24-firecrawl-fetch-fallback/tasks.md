# Tasks

> TDD: each implementation task starts with a failing test. Verification commands: `bunx vitest run <pattern>`; overall gate is `bun run lint && bun run type-check && bun run quality-check`.

## 1. Settings model and plumbing

- [x] 1.1 Write failing tests for `normalizeLoadedSettings` defaults (`firecrawlFallbackEnabled`/`firecrawlAutoAddMapping` → true, key undefined, legacy `proxyList`/`proxySiteMapping` unchanged), then add the three fields to `AppSettings`, `DEFAULT_SETTINGS`, and store getters and setters; verify with `bunx vitest run settings`
- [x] 1.2 Write a failing round-trip test that `copyOptionalAppSettingsFields` carries all three Firecrawl fields plus a `'firecrawl'` token inside `proxySiteMapping`, then add them in `settings-parsers.ts`; verify the parser/sync tests pass
- [x] 1.3 Add `GlobalConfig.getFirecrawlApiKey/getFirecrawlFallbackEnabled/getFirecrawlAutoAddMapping` with tests showing they reflect store updates immediately; verify with `bunx vitest run global-config`
- [x] 1.4 Add `FIRECRAWL_MAPPING_TOKEN` to `src/constants/proxy.ts` and a store action `promoteFirecrawlForSite(rootDomain)` (create `{enabled:true, proxies:['firecrawl']}`, or move/insert at index 0 keeping order), with tests for new domain, existing mapping, and already-first; verify tests pass

## 2. Firecrawl client (`src/services/firecrawl/`)

- [x] 2.1 Capture a real keyless daily-limit response (or confirm from Firecrawl docs) and save it as a test fixture; record the matcher decision in design.md D3; verify the fixture file exists
- [x] 2.2 Write failing tests for `scrape()` request shape (auth header with/without key, `formats`, `maxAge:0`, `onlyMainContent`, forwarded `headers`, abort before send), then implement; verify with `bunx vitest run firecrawl-client`
- [x] 2.3 Write failing tests for response handling: target `metadata.statusCode >= 400` → `FirecrawlTargetError(status)`, empty content → `FirecrawlEmptyContentError`, 402 → `FirecrawlQuotaError`, keyless limit fixture → `FirecrawlQuotaError`, no keyless retry when a key is set; then implement; verify tests pass
- [x] 2.4 Write failing tests (fake timers) for the shared limiter: concurrency cap across independent callers, sliding-window rate, 429 `Retry-After` honored, bounded retries → `FirecrawlRateLimitError`, aborted waiters dequeued, and the 75s HTTP timeout starting only once a request leaves the queue (body sends `timeout: 60000`); then implement; verify tests pass
- [x] 2.5 Write failing tests for `getCreditUsage` (success mapping, 401 → invalid-key result, no request without key) and `search()` (description → snippet mapping, same error semantics), then implement; verify tests pass
- [x] 2.6 Write failing tests for the quota latch (after a quota error the next request fails with no HTTP call; cleared by a key change, by `getCreditUsage` reporting remaining > 0, or after 60 minutes with fake timers), then implement; verify with `bunx vitest run firecrawl-client`

## 3. Page-fetch attempt chain

- [x] 3.1 Write failing fixture tests for `isChallengePage` (Cloudflare "Just a moment" + `cf-chl`, "Attention Required", a real syosetu chapter and a kakuyomu page containing the word "challenge" → false), then implement; verify with `bunx vitest run page-transport`
- [x] 3.2 Write a failing table-driven test for the pure `resolvePlan` (web proxy on/off × Electron × no mapping / CORS mapping / firecrawl-first mapping × fallback on/off × `skipExternalProxy`), covering "firecrawl token skipped when fallback off", "Electron ignores CORS entries" and "Electron ignores stored `proxyEnabled: true` and the default proxy", then implement; verify tests pass
- [x] 3.3 Write failing tests for blocked/transient classification (403/challenge → blocked, not retried; network/timeout/429/5xx → retried once, then blocked; 404/410 → neither), then implement in `proxy-service.ts`; verify tests pass
- [x] 3.4 Rewrite `executeWithAutoSwitch` as the D4 chain (primary → single transient retry → Firecrawl callback → promote mapping when auto-add is on; no global-list rotation; `proxyAutoSwitch` ignored). Update the existing proxy-service tests to the spec scenarios (default succeeds = 1 request; unmapped custom proxy B never requested; fallback disabled → original error; firecrawl-mapped failure doesn't try CORS entries); verify with `bunx vitest run proxy-service`
- [x] 3.5 Make `getProxiedUrl`/`resolveExternalProxyUrl` skip the token; add a regression test that a `['firecrawl', corsUrl]` mapping resolves to the CORS URL and never contains `firecrawl`; grep all `getProxiesForSite` consumers and cover or fix each; verify tests pass
- [x] 3.6 Wire Firecrawl into `fetchScraperPage` for both the browser and Electron paths (rawHtml, forward `extraHeaders`, snapshot with `transportUrl` marking Firecrawl, `status`, `responseUrl` from `metadata.url`), and throw `BlockedResponseError` on challenge pages; save real keyless Firecrawl `rawHtml` captures for syosetu.org, ncode, kakuyomu and novel18 as fixtures and confirm each site's parser gets a non-empty chapter and TOC from them; verify with page-transport and scraper fixture tests for both platforms using mocked axios / `electronAPI.fetch`
- [x] 3.7 Update novel18 per the delta spec: `skipExternalProxy` still allows the Firecrawl fallback with `Cookie: over18=yes` in Firecrawl `headers`; an age-confirmation page from Firecrawl fails closed; verify with the novel18 scraper tests (existing ones updated plus new ones)
- [x] 3.8 Write failing tests that book sync's update check and apply both stop on `FirecrawlQuotaError` (no further entries fetched, results so far kept, recipe **not** invalidated, apply writes fetched chapters and reports the rest as `FIRECRAWL_QUOTA` failures) and that the import chapter batch stops taking new chapters on it while keeping saved ones; then implement per design D9 in `replay.ts`, `book-sync-service.ts`, `import-extraction-service.ts`, and `import-batch-runner.ts`; verify with `bunx vitest run firecrawl-book-sync book-sync import-chapter-batch`
- [x] 3.9 Change `fetchUrlViaPuppeteer` in `src-electron/electron-main.ts` to report the status of the last main-frame document response before capture (and update the preload/`electron.d.ts` types if needed), and make `fetchViaElectron` throw an error that carries that status; verify with a unit test of the status-tracking helper (403 → 200 challenge resolution reports 200, persistent 403 reports 403) and the page-transport Electron tests from 3.3
- [x] 3.10 Parse syosetu.org's new TOC layout (`section.episode-list`: `li.episode-list__chapter` = volume, `a.episode-list__link` = episode; publish time from `<time>`, revision time from `.episode-list__revision[title]`, both normalized to 年月日 for the existing date parser), keeping the old `<table>` path; verify with `bunx vitest run syosetu-scraper` against the real Firecrawl capture (36 episodes, 4 volumes)

## 4. Settings UI

- [x] 4.1 Write failing tests for a `useFirecrawlKeySettings` composable (save validates via credit usage; 401 → not saved + "无效的 API Key"; network error → not saved; empty → removed with no request; manual refresh sends one request; no request on mount), then implement; verify tests pass
- [x] 4.2 Add the Firecrawl card to `ApiKeysSettingsTab.vue` (masked key, 保存, 检查额度 disabled without a key, credits/period display, keyless note, master switch with privacy text covering both uses); verify in the browser preview at `/settings` on web and via an Electron-mode render test
- [x] 4.3 Extract the mapping state and actions from `useProxySettings` into `useSiteMappingSettings` (with a Firecrawl option that stores the token and is displayed as "Firecrawl"); verify by moving or adding composable tests and checking that `useProxySettings` tests still pass
- [x] 4.4 Create `SiteMappingSettingsTab.vue` (auto-add switch disabled with a note when the master switch is off; mapping table; CORS entries shown inactive on Electron) and move `SiteMappingEditDialog` under it; remove the auto-switch switch, auto-add switch and mapping section from `ProxySettingsTab.vue`, and keep Firecrawl out of the default proxy selector; verify in the browser preview
- [x] 4.5 Write failing tests for the new tab lists and all four saved-index ↔ tab-value tables (every old saved index on web and Electron maps to the same logical tab), then update `useSettingsPage.ts` and the panel maps; verify with `bunx vitest run settings-page`

## 5. AI web tools

- [x] 5.1 Write failing tests for context-dependent availability: assistant tool lists include web tools when there's a Tavily key OR the fallback is on; `getTranslationTools`, `getToolsExcludingTranslationManagement` and `getSingleParagraphPolishTools` include them only with a Tavily key. Then add the `allowFirecrawlOnly` flag and pass it through `getAllTools` (design D10); verify with `bunx vitest run tool-registry tools`
- [x] 5.2 Write failing tests for provider order in `search_web` and `fetch_webpage` (Tavily success → no Firecrawl; 432/433/429/5xx/network/401 with fallback on → Firecrawl; fallback off → Tavily error; no Tavily key → Firecrawl directly; zero Tavily results → no fallback), then implement; verify with `bunx vitest run web-search-tools`
- [x] 5.3 Write failing tests for result normalization (`results[{title,snippet,url}]`, no `answer` from Firecrawl, `provider` field, `fetch_webpage` title from metadata and text capped at 50,000 characters) and failure messages (quota with key vs keyless daily limit, rate limit, target error), then implement; update tool descriptions and prompts that say a Tavily key is required; verify tests pass
- [x] 5.4 Write failing tests that the import agent's `search_web` (`ImportMetadataService.prepareSearch`) uses Firecrawl when there's no Tavily key, keeps recording results as metadata-only sources, and rejects with no results recorded when aborted while queued or in flight; implement signal passthrough in `searchWeb`; verify with `bunx vitest run import-metadata`

## 6. Docs and final verification

- [x] 6.1 Update the help docs for the proxy settings, the new 网站映射 tab, and the Firecrawl card in API Keys (keyless limits, privacy, credit check); verify that help-doc tests / the help-docs query index pick up the pages
- [ ] 6.2 Browser end-to-end check in the preview: with default settings, import `https://syosetu.org/novel/375522/6.html` on the web and confirm the chapter parses and `syosetu.org → ['firecrawl']` appears in 网站映射; then turn the master switch off and confirm the import fails with the 403 error. Follow the dev-profile caution (don't trigger sync). Then in `bun run dev:electron`, import one kakuyomu and one ncode chapter and confirm the Electron logs show navigation to the original URL (not `cors.rozx.moe`), and that syosetu.org either succeeds directly or falls back to Firecrawl — *Web part verified 2026-09-24 (CORS 403 → keyless Firecrawl, 607 paragraphs, mapping auto-added; fallback off → 403). Electron part waived by the user.*
- [x] 6.3 Run `bun run test && bun run lint && bun run type-check && bun run quality-check` and fix any failures introduced by this change
