# 上下文改造验证（2026-09-24）

## 实施与数据保护

- 模型目录为离线动态加载快照：111,083 字节、7 个 provider 表、2,919 个模型条目；常用 provider 外的条目按规范化 id 保守合并。
- 目录内 `gpt-4o` 在未填凭据的浏览器表单中获取 128,000 / 16,384；目录外 `gpt-6-sol(high)` 经真实路由探测，明确提示“模型自述，可能不准确”。未保存这些表单改动。
- 度量统一使用厂商 inputTokens 锚点及增量估算；按模型 EMA 校准。历史使用规范 JSON 指纹，不因字段顺序变化失效；API 历史保留 null、reasoning 和厂商元数据。
- 助手与导入在发送前、工具循环步间共用保留近期历史的压缩算法；更新结构化摘要，不拼接旧摘要，不进行降级截断。执行模式不再按估算硬停，导入外层不再重复恢复已失败的请求。
- 普通结果与压缩结果通过同一会话入口一次保存摘要、历史、索引与锚点。超过 512,000 字符仍尝试完整保存；配额失败保留原状态。普通会话写入也不再通过丢弃会话或截断消息来重试。

## 真实助手：两次预判压缩

会话 `f57dbc6f-bb21-4844-b16c-e67c51512b97`，模型 GPT-6 Luna，临时手动窗口 24,000。真实输入框发送，通过原生 ask_user 对话框选择并提交“保留原文汉字”，随后调用 list_help_docs。首次三次请求，最终 inputTokens 6,290，保存 6 条 API 历史。

在两次后续发送前分别加入有界合成资料，真实界面各产生一次完整的“聊天正在总结中 → 已完成对话总结”过程：

| 轮次   | 摘要请求 | 主请求 | 摘要长度 | kept API 历史 | 实测 inputTokens |
| ------ | -------- | ------ | -------- | ------------- | ---------------- |
| 第二轮 | 2 段     | 1      | 312 字符 | 2 条          | 3,939            |
| 第三轮 | 2 段     | 1      | 345 字符 | 2 条          | 3,970            |

第三轮摘要仍含原始问题、最终答案“保留原文汉字”、栞与蓮、CTX-BOOK-123，并把下一步更新为第三章。每份摘要只有一套固定小节。最终回复继续遵守姓名规则。可见原文和两个总结气泡均保留。

桌面用量实测显示 `17% · 4,013 / 24,000`（锚点加最终回复的估算增量）；手动窗口为未知时显示 `4,010 Tokens`，没有百分比；切换至无有效锚点的模型时显示 `5% · ≈6,641 / 128,000`。旧任务也用 ≈ 标识历史估算。

- [实测用量截图](/Users/rozx/.codex/visualizations/2026/09/24/01a0d477-c01a-7001-a533-d24e76061034/context-measured.png)
- [未知窗口截图](/Users/rozx/.codex/visualizations/2026/09/24/01a0d477-c01a-7001-a533-d24e76061034/context-unknown.png)
- [估算值截图](/Users/rozx/.codex/visualizations/2026/09/24/01a0d477-c01a-7001-a533-d24e76061034/context-estimated.png)

## 真实超限与失败保护

客户端手动窗口不能改变服务端的物理上限。为确保测试真实错误恢复，将本次测试模型窗口设为未知以关闭预判，再用 1,200,000 次 ` hello` 构造超长合成输入，不使用用户书籍正文。

- 可压缩的工具结果：7,200,332 字符；主请求收到真实 context window 错误 → 一次摘要 → 一次主请求重试成功，最终回复 CTX-RECOVERED，共 3 次请求。
- 超长 pinned 用户请求：7,230,096 字符；初次主请求失败 → 一次摘要 → 主请求仍失败，共 3 次请求。没有第三次主请求，提示“请新建会话或改用更大窗口的模型”。
- 两条路径均核对调用方原始历史与 localStorage 会话存储完全未变。错误原文、usage 与计数见 context-progress.json。

## 真实导入：自动与手动压缩

独立任务 `c564aee1-f57d-475e-b892-11cc988d816b`，登记一个 example.com 合成来源，仅作登记、不获取网页。长历史用于测试压缩，模型被要求不调用工具、不修改来源和草稿。

- 自动压缩：3 段摘要 + 1 次主请求；检查点保留 system、当前 user 与回复，共 3 条，实测 inputTokens 9,409，记录一个自动摘要事件。
- 手动压缩：1 次摘要请求；检查点只保留当前 user，清空旧锚点，追加手动摘要事件。
- 两次均核对 1 个来源、草稿与既有事件原样保留；新事件只追加。任务正常回到 paused，compacting 标记清除。
- “上下文暂停后手动压缩成功自动继续”另由导入回归测试覆盖；恢复锁在发起新运行前释放。

临时模型参数、默认路由、服务拦截、测试开关与 viewport 均已恢复。独立合成会话和导入任务保留供复核。

## 自动化验证

最终检查结果及唯一 SDK 默认实现的复测记录在 tasks.md 收尾记录中。旧的截断/降级摘要/估算硬停断言已替换为新契约；工具轮次上限、取消、问答恢复、原子存储失败等安全断言保留。新增失败回归还覆盖规范 JSON 指纹与工具历史 null 值，避免实测锚点在下一次读取时失效。

## 唯一 SDK 与锚点修复后的最终复测

在没有 localStorage 测试开关的情况下，默认服务确认为 AiSdkAIService。使用原来的 GPT-6 Sol 助手路由从真实输入框完成 list_help_docs 工具调用，返回 CTX-ANCHOR-OK；持久化 6 条 API 消息（含一个工具结果），请求锚点 inputTokens 为 6,480，重建后的历史完全相同，统一测量为 6,495、estimated=false。桌面显示 `1% · 6,495 / 500,000`，没有近似标记。

GPT-6 Luna 重新完成验证书籍中的四段翻译，任务 status/workflowStatus 均为 end；4/4 已选译文读回成功，每段原来的译文版本保留（现有 2 个版本）。任务面板写入实测 14,689 tokens、有效窗口 922,000、2%、contextEstimated=false，证明 usage 已穿过翻译工具循环。

这次复测的临时服务拦截没有采集请求数，结论依据真实界面、任务状态和落盘数据，不推测调用次数。记录见 [default-sdk-progress.json](default-sdk-progress.json)。

测试期间模型页会清理未被标为“助手可用”的临时路由，已将原始四条任务路由明确保存还原并在重载后核对：翻译和助手使用 GPT-6 Sol，校对和术语使用 GPT-6 Luna。所有临时请求拦截、窗口尺寸覆盖及测试标记已清理。

![工具历史重建后的实测用量](/Users/rozx/.codex/visualizations/2026/09/24/01a0d477-c01a-7001-a533-d24e76061034/context-tool-anchor-final.png)
