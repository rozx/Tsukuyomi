## 1. Implementation

- [x] 1.1 Update AI System Prompts to explicitly instruct the model to output state changes in a dedicated, separate JSON object without content.
- [x] 1.2 Update AI System Prompts to instruct the model to output translation content in a separate JSON object.
- [x] 1.3 Validate `stream-handler.ts` can gracefully handle these isolated chunks (verify current behavior supports it).
